name: adaptifit
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  cloud_firestore: ^6.0.1
  device_preview: ^1.3.1
  firebase_auth: ^6.0.2
  firebase_core: ^4.1.0

  flutter:
    sdk: flutter
  http: ^1.1.0 
  provider: ^6.1.5+1
  table_calendar: ^3.0.9 
  intl: ^0.20.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: "^0.13.1"
  flutter_native_splash: "^2.4.0"

flutter:
  uses-material-design: true
  assets:
    - assets/images/

# Add these lines at the end of the file

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/adaptifit_icon.jpeg"

flutter_native_splash:
  color: "#FFFFFF"
  image: assets/images/adaptifit_icon.jpeg
  android: true
  ios: true
